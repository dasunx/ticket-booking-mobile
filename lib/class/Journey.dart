class Journey {
  final String startingPlace;
  String endingPlace;
  final DateTime startTime;
  DateTime endTime;
  double cost;

  Journey(
      {this.startingPlace,
      this.endingPlace,
      this.startTime,
      this.endTime,
      this.cost});
}
